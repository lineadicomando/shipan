/**
 * Who made this, said once.
 *
 * **A name in the markup and nowhere on the page is not a byline.** The
 * question a reader is owed — and the first of the three the helpful-content
 * guide asks — is *who* wrote this, and an answer that only a crawler can see
 * is an answer given to the wrong party. So the name is spent twice from
 * here: in the footer, where a reader meets it, and in the structured data,
 * where it is the same claim declared rather than parsed. Two places, one
 * fact, and neither can drift from the other.
 *
 * **It is not translated and takes no article.** A handle is a name, and a
 * name does not translate — `docs/i18n.md` argues that about 休門 and it holds
 * here for the same reason. The sentence around it differs by vernacular and
 * arrives from the catalogs with `{author}` in it.
 *
 * **The handle was already in this repository**, as the owner of the address
 * `source.ts` falls back to for the licence's §13 offer. That is not a
 * coincidence to be exploited: the address there is read from
 * `PUBLIC_SOURCE_URL` so a fork points at itself, and deriving a byline from
 * it would put a fork's owner under this project's prose. The name is written
 * out.
 *
 * No `sameAs`, no organisation, no profile. Those are further claims and this
 * is the one that was asked for; what a page may say about itself is bounded
 * by what somebody is prepared to stand behind, which is the whole subject of
 * `docs/sources.md` turned on the site rather than on the engine.
 */
export const AUTHOR = 'lineadicomando';
